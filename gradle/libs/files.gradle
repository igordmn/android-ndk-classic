ext.loadPropertiesIfExists = { String localPath ->
    File file = new File(rootProject.projectDir, localPath)
    if (file.exists()) {
        Properties properties = new Properties()
        properties.load(new FileInputStream(file))
        return properties
    } else {
        return null
    }
}